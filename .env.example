# Postgres/Neon-connectiestring (Vercel Storage → Postgres injecteert dit
# automatisch in productie). Zonder deze variabele werkt de app gewoon lokaal
# via localStorage — een database is niet nodig om te ontwikkelen of te testen.
DATABASE_URL=
